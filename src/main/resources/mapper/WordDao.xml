<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzy.translation.dao.WordDao">
    <insert id="insertWord" useGeneratedKeys="true" keyProperty="id" keyColumn="id" parameterType="com.zzy.translation.entity.Word">
        INSERT INTO
        tb_word(id, open_id, query, trans_src,
        trans_dst, from, to, status_code, create_time,
        last_edit_time)
        VALUE
        (#{id}, #{openId}, #{query}, #{transSrc}, #{transDst}, #{from}, #{to}, #{statusCode}, #{createTime}, #{lastEditTime})
    </insert>
</mapper>